import { Translations } from "../../../../../types/translations";

export const translations: Translations = {
  title: "Remotes",
  branchContextMenu: {
    fetch: {
      value: "Fetch ${branch}",
      args: ["branch"]
    }
  }
};
