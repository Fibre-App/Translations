{
  "$id": "https://fibre-app.com/translation.schema.json",
	"$schema": "http://json-schema.org/draft/2019-09/schema#",
  "title": "Person",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the language"
    },
    "extends": {
			"type": "array",
			"description": "The language tags for the languages this one should extend. They should be in this format: https://en.wikipedia.org/wiki/Language_localisation#Language_tags_and_codes",
      "items": {
        "type": "string"
      }
    },
    "values": {
      "description": "The translation values. The translation placeholders are the keys and the values are the translations to be returned.",
      "type": "object"
    }
  },
  "required": [
    "name",
    "extends",
    "values"
  ]
}