import { Translations } from "../../../../../types/translations";

export const translations: Translations = {
  title: "Repository Bar",
  availableScripts: "Available Scripts",
  remove: {
    value: "Remove ${name}",
    args: ["name"]
  }
};
